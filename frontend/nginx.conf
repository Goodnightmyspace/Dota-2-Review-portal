server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # Дополнительно: можно настроить логирование или сжатие
    error_page 500 502 503 504 /50x.html;
}